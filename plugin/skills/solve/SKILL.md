---
description: Concede the current Spotter exercise — let the agent complete it.
disable-model-invocation: false
---

# Spotter: Concede

The user has conceded. Complete the implementation. Briefly note the key pattern they should remember (1–2 sentences). Then call `mcp__spotter__spotter_exercise` with `action: "deactivate"` to clean up, and resume the original task.
